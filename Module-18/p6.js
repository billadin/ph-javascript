// Create an array for all the mobile phones, display all elements of the array by while loop
let myLearning  = ['Nokia', 'Oneplus', 'Pixel', 'Huawei'];


let counter = 0;

while (counter < myLearning.length) {
    console.log(myLearning[counter]);
    counter++;
}