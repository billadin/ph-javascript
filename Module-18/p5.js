//Decalre an array for all the topics you have learned last few days and display them as output
let myLearning  = ['HTML', 'CSS', 'Tailwind', 'DaisyUI', 'Javascript'];

for (let i = 0; i < myLearning.length; i++) {
    console.log(myLearning[i]);
}

let counter = 0;

while (counter < myLearning.length) {
    console.log(myLearning[counter]);
    counter++;

}